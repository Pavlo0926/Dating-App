import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
  },

  columnContainer: {
    flex: 1,
    overflow: "hidden",
  },

  rowContainer: {
    flexDirection: "row",
  },
});
